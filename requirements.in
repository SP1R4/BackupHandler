# Direct runtime dependencies — single source of truth for the wheel.
#
# Transitive dependencies are pinned in requirements.txt, which is
# generated from this file:
#
#   uv pip compile requirements.in -o requirements.txt
#   # or:
#   pip-compile requirements.in -o requirements.txt
#
# Optional extras (argon2id KDF, dev, test, security) live in pyproject.toml.

boto3>=1.34.0
colorama>=0.4.6
cryptography>=45.0.0
keyring>=25.0.0
paramiko>=3.5.0
pyzipper>=0.3.6
pyTelegramBotAPI>=4.27.0
requests>=2.32.0
retrying>=1.3.4
tqdm>=4.66.0
